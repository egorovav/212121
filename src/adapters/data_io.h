#include "../entities/consts.h"

int input(int data[ROWS][COLLS]); // kerenbea // Работает с входным потоком, принимает стартовую матрицу, заполняет ее, проверяет входные данные на валидность, в противном случае возвращает -1, чтобы вывелась ошибка
void output(int current_data[ROWS][COLLS]); // kerenbea // Красивый вывод матрицы
